# Session Summary

**Date:** 2026-04-06

## Work Completed
- **Level 3 Replay investigation trace integration**: Successfully linked replay investigation bookmarks to the `debug_traces` substrate.
- Created migration `014_trace_investigation_anchors.sql` to track expected/observed logic deltas at the distinct debug-trace step level.
- Extended the `GET /api/v1/backtests/runs/.../debug-traces` API to aggregate anchors directly into the trace.
- Created API endpoint `POST /api/v1/backtests/runs/{run_id}/debug-traces/{debug_trace_id}/investigation-anchors` to create scenario annotations while deferring the full `research.annotations` pipeline integration.
- **UI Phase B**: Restructured the `/monitoring` Backtests workspace from a single, long-scrolling page to a tabbed sub-workspace interface (Launch, Runs, Compare, Investigate). 
- Refactored `index.html` to replace anchor links with workspace tab buttons and grouped panels accordingly. Added JS logic for smooth tab switching.
- Kept the rollout trackable and updated AI-memory workflow states for the handoff.

## Decisions Made
- Chose to create a direct junction schema `research.trace_investigation_anchors` rather than immediately wiring into the generic annotation service. The `debug-trace-rollout-plan.md` specifically requested deferring full replay-note integration in favor of simple trace-level investigation linkage.
- For UI Phase B, chose a pure frontend toggle strategy (CSS classes and vanilla JS) for the Backtests tab hierarchy to avoid destabilizing the backend APIs or introducing complex framework dependencies.

## Validation Completed
- Python static compilation and local DB test integration verified clean against `014..` layout and syntax.
- Visual browser verification completed for the new Backtests tabs; all four workspace modes (Launch, Runs, Compare, Investigate) confirmed fully functional and isolated.

**Date:** 2026-04-07

## Work Completed
- **Session Start**: Summarized repo state and created implementation plan for profiling/optimizing backtest runner performance.
- **Backtest Engine Performance**: Cut per-bar framework overhead by half. Bypassed redundant mark_to_market invocations during empty intents via incremental equity short-circuit. Replaced heavy per-bar astimezone date boundaries with predictive UTC floating bounds.

**Date:** 2026-04-08

## Work Completed
- Normalized display-only scientific-zero strings like `0E-12` to `0` across the current `/monitoring` UI formatting path, covering JSON shells, tables, and compact property-card summaries without changing stored backend values.
- Verified the front-end change with `node --check frontend/monitoring/app.js`.
- Added a run-level expected-vs-observed aggregate view on top of the landed trace-note baseline, so one backtest run can now summarize investigation-note counts, statuses, note sources, scenario tags, and linked trace rows without opening every note individually.
- Exposed that aggregate view at `GET /api/v1/backtests/runs/{run_id}/expected-vs-observed` and wired the internal `/monitoring -> Backtests -> Investigate` workspace to show a summary block plus a clickable note table.
- Added focused regression coverage for the new aggregate API resource and for DB-backed overview aggregation across persisted trace notes.
- Implemented the first trace-level replay investigation-note foundation by reusing `research.annotations` for `debug_trace`-scoped notes instead of introducing a second note store.
- Added `TraceInvestigationNoteService`, trace-note list/write API endpoints, and a repository helper for loading one persisted trace row plus its existing investigation anchors.
- Added focused regression coverage for trace-note request validation, trace-note API resources, and DB-backed service behavior on top of persisted debug traces.
- Extended `/monitoring -> Backtests -> Investigate` with minimal trace-note list/detail/write surfaces so investigation conclusions can now be saved next to the selected debug trace.
- Updated repo state/planning docs to reflect that replay investigation anchors and first trace-level investigation notes are now landed, while replay timeline and broader expected-vs-observed workflow remain future work.
- Re-read the required session-start docs before making any code changes: `README.md`, `docs/spec-index.md`, `docs/implementation-plan.md`, `docs/phases-2-to-9-checklists.md`, `docs/repo-self-review-tracker.md`, and the core `docs/agent-memory/*` files.
- Verified the current repo state is consistent across README, planning docs, and memory docs: Phase 1-4 are implemented, Phase 5 has a substantial backtest/diagnostics/compare/debug-trace foundation, and the main open frontier remains replay-note/evidence workflow rather than another basic trace slice.
- Chose one highest-value candidate subtask for the next implementation step: extend the landed replay/debug-trace anchor substrate into replay-note and expected-vs-observed evidence flow.
- Identified the first files/specs to inspect before coding that slice: `docs/debug-trace-rollout-plan.md`, `docs/object-level-notes-and-annotations-spec.md`, `docs/backtest-and-replay-diagnostics-spec.md`, `docs/strategy-workbench-spec.md`, `src/backtest/compare_review.py`, `src/storage/repositories/research.py`, `src/backtest/traces.py`, and `src/api/app.py`.
- Re-read the required repo entry docs plus agent-memory files before touching code.
- Verified that replay trace anchors already exist in code (`research.trace_investigation_anchors`, anchor write endpoint, and `/monitoring` wiring), but the repo memory docs were out of sync about that state.
- Chose one highest-value subtask for this session: harden and reconcile replay/debug-trace investigation linkage rather than starting a new Phase 5 slice on stale assumptions.
- Hardened replay trace-anchor writes so they now reject empty payloads and reject `debug_trace_id` values that do not belong to the addressed `run_id`.
- Added focused regression coverage for request validation, nested run/trace validation, debug-trace resource mapping, and repository-level anchor aggregation.
- Reconciled the replay-trace rollout and agent-memory docs so they now reflect the verified state: replay-investigation anchors are landed, while replay notes/runs and broader expected-vs-observed workflow are still pending.
- Re-read repo state again before debugging a new Phase 5 regression in `POST /api/v1/backtests/runs`.
- Traced the new 500 to a local change in `src/backtest/data.py` that tried to read `session.strategy_required_bar_history`, which does not exist on `StrategySessionConfig`.
- Patched the loader/runner contract so preload history now comes from the instantiated strategy's `required_bar_history`, matching the existing runner history-cap behavior.
- Added a focused regression test proving the bar loader can preload the requested lookback window without requiring a new session-schema field.
- Re-read repo state before starting the next Phase 5 slice and chose one subtask: complete the `btc_hourly_momentum@v1.0.0` launch path.
- Verified that the missing piece was formal DB bootstrap seed coverage, not strategy registry or basic UI selection wiring.
- Added a formal bootstrap migration for `btc_hourly_momentum@v1.0.0`, aligned the helper seed script to the same version metadata, and exposed an `Hourly Perp` preset in `/monitoring`.
- Extended the seed-default regression so the repo now verifies `btc_hourly_momentum@v1.0.0` exists alongside the other seeded strategy versions.
- Re-read repo state again before starting the next hourly follow-up and chose one subtask: prove `btc_hourly_momentum` can run a fully persisted backtest from minute bars.
- Added a focused Phase 5 regression test that drives `HourlyMovingAverageCrossStrategy` from persisted `1m` BTC perp bars and verifies the persisted run, orders, fills, timeseries, and performance summary are all written as expected.
- Validated the new persisted-hourly path with `python -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_hourly_strategy_can_persist_run_from_minute_bars -v`.
- Chose one follow-up subtask after that runner-level proof: verify the same hourly path through the real backtest API create/detail surfaces rather than only through direct runner calls.
- Added an API integration-style regression that seeds minute bars, routes both `transaction_scope()` and `connection_scope()` through one rollback-only test connection, and validates `POST /api/v1/backtests/runs` plus detail/orders/fills/signals/timeseries for `btc_hourly_momentum`.
- Validated the API hourly end-to-end path with `python -m unittest tests.test_api_models.ModelsApiTests.test_backtest_run_create_can_launch_persisted_hourly_run_end_to_end -v`.

**Date:** 2026-04-11

## Work Completed
- Re-read the required session-start docs before any implementation work: `README.md`, `docs/spec-index.md`, `docs/implementation-plan.md`, `docs/phases-2-to-9-checklists.md`, `docs/repo-self-review-tracker.md`, and the core `docs/agent-memory/*` files.
- Verified the tracked repo state is still consistent across README, plan/checklist docs, self-review findings, and handoff memory: Phase 1-4 are implemented, Phase 5 has substantial backtest/diagnostics/compare/debug-trace/investigation groundwork, and the most valuable open frontier remains evidence/workbench maturity rather than a new execution phase.
- Chose one current highest-value subtask before coding: improve compare/analyze maturity through durable persisted compare-set workflow and richer risk/runtime diff visibility, which is also the clearest open follow-up called out by the self-review tracker and task memory.
- Identified the first files/specs to inspect for that subtask before implementation: `docs/strategy-workbench-spec.md`, `docs/backtest-and-replay-diagnostics-spec.md`, `docs/object-level-notes-and-annotations-spec.md`, `docs/repo-self-review-tracker.md`, `src/backtest/compare.py`, `src/backtest/compare_review.py`, `src/storage/repositories/research.py`, `src/storage/repositories/backtest.py`, `src/api/app.py`, and `frontend/monitoring/app.js`.
- Extended the compare projector so selected runs now carry diagnostics/risk facts per run, and compare sets now emit first-class diagnostics diffs covering blocked-intent counts, guardrail-state snapshots, outcome/block counts, and surfaced diagnostic-flag differences.
- Extended persisted compare snapshots, compare-review seed facts, API compare resources, and `/monitoring` compare rendering so the richer diagnostics/runtime-risk compare layer is available end to end instead of staying buried in per-run diagnostics.
- Verified the new compare slice with `python -m py_compile src/backtest/compare.py src/backtest/compare_review.py src/api/app.py`, `node --check frontend/monitoring/app.js`, `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_compare_projector_supports_side_by_side_run_analysis -v`, and `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_api_models.ModelsApiTests.test_backtest_compare_sets_endpoint_returns_compare_projection -v`.
- Updated the self-review tracker and Phase 5 planning/checklist docs to reflect that compare/analyze now includes persisted diagnostics/runtime-risk diff visibility rather than only KPI/assumption comparison.
- Followed with a compare-workspace UI cleanup in `/monitoring`: added compare summary cards, split assumption diffs from diagnostics/risk diffs into separate tables, and made the compare tab read summary-first instead of presenting one mixed diff table plus raw payload first.
- Re-verified the front-end compare follow-up with `node --check frontend/monitoring/app.js` and a fresh `py_compile` pass over the touched backend files to ensure the UI-only slice did not regress the compare API path.
- Added a compare-specific selected-run drill-down inside `/monitoring -> Backtests -> Compare`, so clicking a compared run row now shows run identity, KPI snapshot, diagnostics/risk evidence, and guardrail state without leaving the compare workspace.
- Re-verified that selected-run compare drill-down follow-up with `node --check frontend/monitoring/app.js` plus a backend `py_compile` pass over the compare API path.
- Added compare diff drill-down inside `/monitoring -> Backtests -> Compare`, so clicking an assumption diff or diagnostics/risk diff row now shows selected-diff summary cards, run-by-run values, and raw diff detail in the same workspace.
- Re-verified the compare diff drill-down follow-up with `node --check frontend/monitoring/app.js` plus a backend `py_compile` pass over the compare API path.
- Added a new repo-aligned strategy design doc at `docs/btc-4h-breakout-perp-research-spec.md`, translating the proposed BTC 4H breakout idea into explicit strategy, risk, execution, feature-input, parameter, and ablation guidance for the current Phase 5 architecture.
- Updated `docs/spec-index.md`, `docs/implementation-plan.md`, and `docs/phases-2-to-9-checklists.md` so the new 4H breakout strategy spec is part of the formal documentation path.
- Added a concrete companion checklist at `docs/btc-4h-breakout-perp-implementation-checklist.md`, turning the new strategy line into an implementation-ready sequence with exact file touchpoints, staged tasks, first tests, seed work, UI work, and `v0.1` done criteria.
- Updated the main doc map again so the 4H breakout line now has both a design spec and an execution checklist wired into the formal planning entrypoints.
- Landed the first runnable `btc_4h_breakout_perp@v0.1.0` implementation slice: added the strategy class and registry entry, 4H aggregation plus trend/breakout/ATR entry logic, a bootstrap seed migration/script, and focused foundation tests for registration, validation, and first entry/no-entry behavior.
- Verified that first breakout slice with `python -m py_compile src\\strategy\\examples.py src\\strategy\\registry.py tests\\test_phase5_foundation.py tests\\test_seed_defaults.py scripts\\seed_4h_breakout_strategy.py`, `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_default_registry_loads_seeded_example_strategy tests.test_phase5_foundation.Phase5FoundationTests.test_four_hour_breakout_strategy_validates_parameters tests.test_phase5_foundation.Phase5FoundationTests.test_four_hour_breakout_strategy_requires_bucket_close_and_sufficient_history tests.test_phase5_foundation.Phase5FoundationTests.test_four_hour_breakout_strategy_enters_on_trend_breakout_and_valid_atr tests.test_phase5_foundation.Phase5FoundationTests.test_four_hour_breakout_strategy_does_not_enter_without_breakout -v`, `& .\\.venv\\Scripts\\python.exe .\\scripts\\seed_4h_breakout_strategy.py`, and `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_seed_defaults.SeedDefaultsTests.test_strategy_version_seed_defaults_exist -v`.
- Added the new breakout-oriented feature-input line `bars_perp_breakout_context_v1`, including derived market-context fields for `minutes_to_next_funding`, `oi_change_pct_window`, `price_change_pct_window`, and `weak_price_oi_push`, while keeping the older `bars_perp_context_v1` path intact.
- Extended the first `btc_4h_breakout_perp` strategy slice to consume the new funding/proximity/OI-vs-price context filters when breakout context is available, and added the named assumption bundle `breakout_perp_research@v1` to point research runs at the new feature-input version.
- Verified the breakout-context follow-up with `python -m py_compile src\\strategy\\base.py src\\strategy\\examples.py src\\backtest\\data.py src\\backtest\\runner.py src\\backtest\\assumption_registry.py tests\\test_phase5_foundation.py` and `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_named_assumption_bundle_registry_resolves_effective_snapshot tests.test_phase5_foundation.Phase5FoundationTests.test_breakout_context_cursor_derives_funding_window_and_change_fields tests.test_phase5_foundation.Phase5FoundationTests.test_four_hour_breakout_strategy_enters_on_trend_breakout_and_valid_atr tests.test_phase5_foundation.Phase5FoundationTests.test_four_hour_breakout_strategy_skips_entry_when_funding_is_too_hot -v`.
- Extended `/monitoring` launch support for the new breakout strategy by adding a `4H Breakout Perp` preset, strategy selector entry, strategy-specific breakout parameter form section, and breakout-aware strategy-param payload assembly in the front-end launch flow.
- Added API-facing regression coverage proving the assumption-bundle list now includes `breakout_perp_research@v1`, `POST /api/v1/backtests/runs` accepts `btc_4h_breakout_perp@v0.1.0`, and a persisted breakout run can complete end to end through create/detail/orders/fills/signals surfaces.
- While validating the persisted breakout path, confirmed that the strategy's ATR-based sizing can exceed the default `perp_medium_v1` quantity guardrails, so the breakout launch preset now carries explicit `max_position_qty` and `max_order_qty` overrides to keep the first research path launchable without changing shared risk-policy defaults.
- Verified the breakout launch/API follow-up with `node --check frontend\\monitoring\\app.js` and `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_api_models.ModelsApiTests.test_backtest_assumption_bundles_endpoint_lists_named_registry_entries tests.test_api_models.ModelsApiTests.test_backtest_run_create_accepts_breakout_strategy_request tests.test_api_models.ModelsApiTests.test_backtest_run_create_can_launch_persisted_breakout_run_end_to_end -v`.
- Cleaned up the `/monitoring` launch form so `btc_4h_breakout_perp` now hides the legacy momentum-only `short_window / long_window / target_qty / allow_short` fields and only shows breakout-specific controls, reducing confusion between the MA-crossover and breakout parameter sets.
- Re-verified the UI-only launch-form cleanup with `node --check frontend\\monitoring\\app.js` plus a backend `py_compile` sanity pass.
- Shifted focus to long-window backtest performance and landed a first global optimization for year-plus runs: `BacktestBarLoader` now exposes an iterator path that merges per-symbol ordered bar streams, and `BacktestRunnerSkeleton.load_and_run()` now consumes that already-sorted iterator directly instead of forcing a full in-memory `list + sort` before the run loop.
- Added focused regression coverage proving the iterator loader preserves cross-symbol time ordering while keeping the existing preload-window behavior and persisted hourly run path intact.
- Verified the long-window loading optimization with `python -m py_compile src\\storage\\repositories\\market_data.py src\\backtest\\data.py src\\backtest\\runner.py tests\\test_phase5_foundation.py` and `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_bar_loader_uses_strategy_required_history_for_preload_window tests.test_phase5_foundation.Phase5FoundationTests.test_bar_loader_iter_bars_merges_symbols_in_time_order tests.test_phase5_foundation.Phase5FoundationTests.test_hourly_strategy_can_persist_run_from_minute_bars -v`.
- Optimized the current high-timeframe breakout strategy path for long-window minute-bar runs: `btc_4h_breakout_perp` now maintains incremental 4H bucket history in strategy-local state instead of rebuilding the full 4H aggregation from `recent_bars` on every minute evaluation.
- Added focused regression coverage proving the incremental 4H path preserves the existing entry/no-entry/funding-filter behavior and does not emit duplicate decisions inside the same closed 4H bucket.
- Verified the breakout incremental-aggregation optimization with `python -m py_compile src\\strategy\\examples.py tests\\test_phase5_foundation.py` and `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_four_hour_breakout_strategy_enters_on_trend_breakout_and_valid_atr tests.test_phase5_foundation.Phase5FoundationTests.test_four_hour_breakout_strategy_does_not_enter_without_breakout tests.test_phase5_foundation.Phase5FoundationTests.test_four_hour_breakout_strategy_skips_entry_when_funding_is_too_hot tests.test_phase5_foundation.Phase5FoundationTests.test_four_hour_breakout_strategy_does_not_reemit_within_same_closed_bucket -v`.
- Reduced long-window persisted-run memory pressure by changing `load_run_and_persist()` to create the run in `running` state first, stream `performance_timeseries` in chunks during execution, and finalize the run metadata/status only after the loop completes.
- Added focused regression coverage for the new persisted-run lifecycle, proving streamed timeseries writes preserve the existing summary/orders/fills/debug-trace/API behavior while avoiding in-memory retention of the full performance-point list.
- Verified the streamed-timeseries persistence change with `python -m py_compile src\\backtest\\performance.py src\\backtest\\runner.py src\\storage\\repositories\\backtest.py tests\\test_phase5_foundation.py`, `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_populates_backtest_tables tests.test_phase5_foundation.Phase5FoundationTests.test_hourly_strategy_can_persist_run_from_minute_bars tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_streams_timeseries_and_finalizes_run -v`, `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_can_persist_compact_debug_traces tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_can_persist_market_context_snapshot -v`, and `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_api_models.ModelsApiTests.test_backtest_run_create_can_launch_persisted_breakout_run_end_to_end -v`.
- Reduced trace-heavy persisted-run memory pressure further by streaming simulated orders, fills, and debug traces during the run instead of keeping full artifact lists in memory until final persistence.
- Added focused regression coverage proving the persisted path now inserts orders on creation, updates order status on fill/expiry, writes fills as they occur, writes debug traces step-by-step, and still preserves the existing persisted backtest API surfaces.
- Verified the streamed artifact persistence follow-up with `python -m py_compile src\\backtest\\runner.py src\\storage\\repositories\\backtest.py tests\\test_phase5_foundation.py`, `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_streams_orders_fills_and_debug_traces tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_can_persist_compact_debug_traces tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_can_persist_market_context_snapshot -v`, `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_populates_backtest_tables tests.test_phase5_foundation.Phase5FoundationTests.test_hourly_strategy_can_persist_run_from_minute_bars tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_streams_timeseries_and_finalizes_run -v`, and `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_api_models.ModelsApiTests.test_backtest_run_create_can_launch_persisted_breakout_run_end_to_end -v`.
- Reduced persisted write round-trips further by batching `debug_traces` inserts and order-status updates inside `BacktestRunRepository` instead of issuing one SQL round-trip per record.
- Re-verified that the batched repository writes preserve trace investigation flows, expected-vs-observed aggregation, streamed persisted artifacts, and the breakout API persisted launch path.
- Verified the batched repository-write follow-up with `python -m py_compile src\\storage\\repositories\\backtest.py`, `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_debug_trace_records_can_aggregate_investigation_anchors tests.test_phase5_foundation.Phase5FoundationTests.test_trace_investigation_notes_seed_system_fact_and_allow_human_follow_up tests.test_phase5_foundation.Phase5FoundationTests.test_expected_vs_observed_overview_aggregates_trace_note_counts tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_streams_orders_fills_and_debug_traces tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_can_persist_compact_debug_traces tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_streams_timeseries_and_finalizes_run -v`, and `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_api_models.ModelsApiTests.test_backtest_run_create_can_launch_persisted_breakout_run_end_to_end -v`.
- Added a first debug-trace compact mode for long-window runs: `debug_trace_stride` and `debug_trace_activity_only` now let backtests keep all activity-bearing steps while sampling quiet background steps more sparsely.
- Wired the new compact-mode controls through run config persistence, runtime debug-trace summaries, the `/monitoring` launch form, and API request validation so the setting is operator-visible instead of being a hidden runner-only knob.
- Verified the compact-mode slice with `python -m py_compile src\\models\\backtest.py src\\backtest\\runner.py src\\storage\\repositories\\backtest.py tests\\test_phase5_foundation.py tests\\test_api_models.py`, `node --check frontend/monitoring/app.js`, `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_run_bars_debug_trace_compact_mode_keeps_activity_and_samples_background tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_compact_debug_trace_mode_persists_sampling_config tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_can_persist_compact_debug_traces -v`, and `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_api_models.ModelsApiTests.test_backtest_run_create_accepts_breakout_strategy_request tests.test_api_models.ModelsApiTests.test_backtest_run_create_can_launch_persisted_breakout_run_end_to_end -v`.
- Added operator-friendly debug-trace preset levels on top of the low-level compact controls: `debug_trace_level` now supports `full`, `compact`, and `sparse`, each mapping to a default stride/activity policy while still allowing explicit overrides.
- Wired the new trace-level preset through run validation, persisted run metadata, runtime debug-trace summaries, `/monitoring` launch controls, and API request coverage so long-window users can choose a trace density without hand-tuning stride values.
- Verified the trace-level preset follow-up with `python -m py_compile src\\models\\backtest.py src\\backtest\\runner.py src\\storage\\repositories\\backtest.py tests\\test_phase5_foundation.py tests\\test_api_models.py`, `node --check frontend/monitoring/app.js`, `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_backtest_run_config_debug_trace_level_applies_defaults tests.test_phase5_foundation.Phase5FoundationTests.test_run_bars_debug_trace_compact_mode_keeps_activity_and_samples_background tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_compact_debug_trace_mode_persists_sampling_config tests.test_phase5_foundation.Phase5FoundationTests.test_load_run_and_persist_can_persist_compact_debug_traces -v`, and `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_api_models.ModelsApiTests.test_backtest_run_create_accepts_breakout_strategy_request tests.test_api_models.ModelsApiTests.test_backtest_run_create_can_launch_persisted_breakout_run_end_to_end -v`.
- Fixed the `/monitoring` launch-form trace controls so selecting `Debug Trace Level` now immediately syncs the visible stride/activity defaults, and manual edits are clearly treated as explicit overrides instead of silently conflicting with the selected preset.
- Simplified the built-in launch presets so they no longer hard-code separate trace-stride overrides on top of a trace level by default, which removes the earlier confusing `compact + 24` style combinations from the UI.
- Verified the UI trace-control sync follow-up with `node --check frontend/monitoring/app.js`.
- Fixed the `/monitoring` launch payload so enabling `Persist Debug Traces` now also enables `persist_signals`, preventing the earlier diagnostics pattern where trace-heavy runs still showed `no_signals_generated` and `signal_link_gap` because signals were never persisted.
- Verified the signal-linkage follow-up with `node --check frontend/monitoring/app.js`, `python -m py_compile tests\\test_api_models.py`, and `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_api_models.ModelsApiTests.test_backtest_run_create_accepts_hourly_strategy_request tests.test_api_models.ModelsApiTests.test_backtest_run_create_accepts_breakout_strategy_request -v`.
- Improved the `/monitoring -> Backtests -> Runs` Period Breakdown workspace so it now has a clearer explanatory header plus an in-place `Month / Quarter / Year` period-type toggle instead of a fixed month-only view.
- Verified the Period Breakdown UI follow-up with `node --check frontend/monitoring/app.js`.
- Clarified backtest performance metric display in `/monitoring` by formatting `Return`, `Annualized Return`, and `Max Drawdown` as percentages across the run list, run summary, compare views, and period breakdown tables.
- Verified the percentage-formatting follow-up with `node --check frontend/monitoring/app.js`.
- Fixed `Period Breakdown` turnover semantics so month/quarter/year rows now return normalized turnover ratios instead of raw traded notional under the same field name.
- Verified the turnover-normalization follow-up with `python -m py_compile src\\backtest\\periods.py tests\\test_phase5_foundation.py` and `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_period_breakdown_supports_month_quarter_and_year -v`.
- Improved `/monitoring` turnover readability by formatting turnover values as phrases like `93.46% of initial cash` across the run summary, compare views, and period breakdown table.
- Verified the turnover-formatting follow-up with `node --check frontend/monitoring/app.js`.
- Improved Backtests cost readability in `/monitoring` by formatting fee and slippage values with grouped digits and money-like decimals across run summaries, compare views, and fill tables.
- Verified the cost-formatting follow-up with `node --check frontend/monitoring/app.js`.
- Added quick Debug Trace filter presets in `/monitoring -> Backtests -> Investigate`, including a one-click `Cooldown Blocks` view that applies `risk_code=cooldown_active` plus `blocked_only=true`.
- Verified the debug-trace preset follow-up with `node --check frontend/monitoring/app.js`.
- Extended persisted debug traces with per-step cooldown risk-state metadata and added a `Cooldown State Changes` investigation path in the API/UI so new runs can filter cooldown activation/active-state traces directly instead of only cooldown blocks.
- Verified the cooldown-state trace follow-up with `python -m py_compile src\\backtest\\runner.py src\\storage\\repositories\\backtest.py src\\api\\app.py tests\\test_phase5_foundation.py tests\\test_api_models.py`, `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_phase5_foundation.Phase5FoundationTests.test_debug_traces_capture_cooldown_state_changes -v`, `& .\\.venv\\Scripts\\python.exe -m unittest tests.test_api_models.ModelsApiTests.test_backtest_run_detail_endpoints_return_orders_fills_timeseries_and_signals -v`, and `node --check frontend/monitoring/app.js`.
